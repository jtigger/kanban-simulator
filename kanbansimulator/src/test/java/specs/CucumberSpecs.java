package specs;

import org.junit.runner.RunWith;

import cucumber.junit.Cucumber;

@RunWith(Cucumber.class)
public class CucumberSpecs {
  /* Required by Cucumber-JVM to indicate that .feature files can be found at this location
   * and in sub-directories.
   * 
   * Step definitions are found in 
   */
}